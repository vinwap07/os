# Лекция 2. 08.09.2026

## Архитектура фон Неймана
Это классический принцип организации компьютерных систем, в основе которого лежит совместное хранение команд и данных в одном общем блоке памяти. 

### Принципы фон Неймана (работа процессора и памяти)
- **Принцип программного управления**. Программа представляет собой упорядоченную последовательность команд, хранящуюся в памяти. Процессор выполняет команды одну за другой, начиная с адреса, указанного в счетчике команды. Исключениями являются явные изменения потока (вызовы подпрограмм, переходы)
- **Принцип условного перехода**. Команды из программы могут выполняться не только одна за другой. Могут существовать команды условного перехода (if-else), которые изменяют последовательность выполнения команд в зависимости от значений данных
> Обычно процессоры при if-else просто проверяют значение 0 или не 0
- **Принцип двоичности**. Все данные в памяти должны храниться в двоичном виде
- **Принцип однородности памяти**. Команды и данные хранятся в одной памяти. Над командами можно выполнять такие же действия, как и над данными.
> Современные компьютеры одновременно как подчинаются данному принципу, так и нарушают его. В современном компьютере память делится на зоны. "Договариваются", что в определенной зоне будут храниться определенные типы данных (команды, константы и т. д.)

- **Принцип адресуемости памяти**. Основная память состоит из пронумерованных ячеек. Процессору в произвольный момент времени доступна любая ячейка 

### Ограничения архитектуры фон Неймана: 
- **Бутылочное горлышко**. Если мы считаем память однородной, то процессору при выполнении каждой команды надо каждый раз обращаться к памяти, чтобы взять данные, а затем снова обратиться к ней, чтобы уже записать их. При обращении к памяти тратится много времени, намного больше, чем процессор выполняет саму команду. Эту проблему на данный момент решают при помощи кэша внутри процессора, который физически намного ближе к процессору.
- **Cамомодифицирующийся код**. Так как данные и команды находятся в одной памяти, то можно изменить код ошибки - по ошибке или специально. До разделения памяти на зоны были популярны вирусы, переписывающие код программ.

## Центральный процессор (ЦП, CPU) 
`Центральный процессор  (central processing unit)` - основное вычислительное устройство компьютера, выполняющее инструкции программы, обарабатывающее данные и управляющиее другими компонентами системы.

Внутри процессора находится набор команд, который он может выполнять - `архитектура набора команд`.

Говоря о процессоре, можно встретить такие термины: 
- `Разрядность` (16, 32, 64 bit). Объем данных в битах, который процессор может обрабатывать за один рабочий такт. 
- `Регистры` (память процессора). Сверхбыстрая ячейка внутренней памяти центрального процессора, которая одновременно хранит данные, адреса и команды для текущих вычислений.  
- `Прерывания`. Остановка выполнения текущей команды для выполнения другой команды.
> Без прерываний невозможно представить компьютер, в котором одновременно работают несколько приложений. Благодаря прерываниям даже старые компьютеры с одним ядром, с одним потоком могли вести работу нескольких приложений
- `Пошаговое исполнение (такт)`. Внутри компьютера существует тактовый генератор, генерирующий электрическое напряжение. Процессор выполняет одну операцию в один такт. За счет тактов происходит синхронизация между элементами компьютера. Также существует понятие `тактовая частота` - число тактов, которые процессор выполняет за одну секунду (ГГц)

### Виды архитектур набора команд
#### CISC (Complex Instruction Set Computer)
Процессор может исполнять какие-то базовые команды. Мы можем ускорить выполнение каких-то конкретных задач за счет определенной программы. Появляется соблазн добавить данную программу в сам процессор. 

> *Пример:* существовал процессор Pentium 166 МГц. После него появился процессор Pentium MMX (Multi Media Extensions), который внутри себя включал команды для работы с мультимедиа. 

Такие процессоры начали называть `процессорами со сложными инструкциями`. 

Другой особенностью таких процессоров была переменная длина команд. 

> Команда безусловного перехода (JMP) требует только код операции и адрес назначения (3 байта). Команда сложения двух чисел в памяти (ADD) требует код операции, адреса первого и второго аргументов (5 байт)

***Главная проблема:*** где кончается одна команда и начинается другая?

Так как в памяти все хранится в виде непрерывного потока байт, процессору на уровне электроники крайне сложно понять, где проходит граница между инструкциями. Возникает опасность сдвига и фатальной ошибки декодирования.  

Реализовывать обработку огромных и сложных инструкций напрямую на аппаратных транзисторах физически невозможно - схема получается слишком громоздкой. Поэтому внутри CISC-процессора используется `микрокод`. Каждая сложная команда, поступающая извне, на самом деле является не просто элементарным действием, а аналогом "вызова функции".

**Регистровый голод**. Площадь кристалла процессора ограничена технологическим процессом. Чем сложнее система команд и чем больше транзисторов уходит на логику декодирования и микрокод, тем меньше физического пространства остается в регистровой памяти. А чем меньше регистров, тем чаще приходится обращаться к оперативной памяти, что является узким горлышком.

> **Прикол:** На языке программирования C можно написать код, работающий чисто на регистрах, без использования оперативной памяти. 
```c
register int a = 5;
register int b = 10;
register int c = a + b;
```

**Уязвимости кэша и проблемы безопасности**. Исторически процессоры оптимизировались под максимальную скорость, из-за чего архитекторы упустили критическую брешь в безопасности: *данные предыдущих программ оставались в кэш-памяти*. 

Чтобы решить данну проблему пришлось внедрить механизм, очищающий кэш после каждой программы, что резко уменьшило производительность процессора на 15-20%.

#### RISC (Reduced Instruction Set Computer)
Анализируя проблемы подхода выше придумали новый подход - RISC
- Простые команды
- Команды имеют фиксированную длину и формат
- Любые операции вида "изменить" выполняются только над содержимым регистров, а уже из регистров данные выгружаются в память
- Большое количество регистров общего назначения
- Отсуствие микропрограмм (микрокода) внутри самого процессора

Возникает резонный вопрос: если в RISC нет сложных специализированных команд, как решать тяжелые мультимедийные например задачи? 

Решение: перенос интеллекта из железа в софт (умные компиляторы). Оказалось, что хорошему компилятору проще составить максимально эффективную последовательность из множества простых RISC-команд, чем человеку или процессору пытаться эффективно использовать одну громоздкую CIRS-команду.  

### Классы машинных инструкций
> Словестные обозначения команд взяты из ассемблера

- Перемещение. `MOV`, `PUSH`, `POP`
- Арифметические. `ADD`, `SUB`, `MUL`, `DIV`
- Логические и побитовые. `AND`, `OR`, `XOR`, `SHL`, `SHR`, `TEST`
- Ввод\вывод. `IN`, `OUT`
- Управление потоком. `JMP`, `JZ` (jump if zero), `JNZ` (jump if not zero), `CALL`, `RET`

> **Отличие JMP и CALL**. При `JMP` просто переходится в другое место, а в `CALL` запомниается место откуда идет вызов, после вызова идет возврат обратно

### Запуск компьютера
При запуске компьютера первым делом отрабатывает `BIOS` из flash-памяти. 

> Раньше не продавали компьютеры готовыми, любители собирали их самостоятельно по инструкциям из журналов. Чтобы компьютер начал работать ему все же нужен BIOS. Конечно любитель сам этот BIOS писать не будет. Поэтому в журналах реально писали длинный шестнадцатеричный код, который переписывали руками. 

Что происходит при запуске компьютера? 
1. Блок питания преобразует переменный ток (220 В) в постоянный. 
2. Блок питания проводит внутреннее тестирования. Если напряжение стабильно, он отправляет на материнскую плату специальный логический сигнал Power Good.
3. Активируется тактовый генератор, который начинает посылать ритмичные импульсы (такты) для синхронизации работ всех микросхем. 
4. Сброс процессора - все регистры пустые, кроме регистра указателя команд.
5. Работа BIOS, самотестирование оборудования. 
6. Запуск операционной системы.  

> Напомним что , процессор ничего, кроме изменения памяти не занимается. Можно сказать, что процессор обрабатывает ввод и вывод, но на деле у процессора просто есть специальные ячейки памяти для ввода и вывода, с которыми процессор работает как обычно - прочитать и записать.

> **Прикол:** можно переделать код из C в ассемблер
``` bash 
gcc -S main.c
```

## Оперативная память
небольшая пластинка, примерно длиной в ручку

она вставляется в материнскую плату. 

с советстких времен исопльзуют термин оперативное запоминающее устройство (ОЗУ), а на англисйском Random Access Memory (RAM) - по сути другое, но сейчас прнято считать синонимами

имеет линейное адресное пространство. нумерация ячеек памяти начинается с унля
размер ячейки памяти 1 байт (8 бит)

информация хранится пока есть электрическое питание, то есть если выключить и включить обратно оперативку, то память будет заполнена нулями

### системная шина
системная делится на 3 части: 
- управления (control bus)
- адреса (address bus)
- памяти (data bus)

никто особо не задумывается что шина это пучок проводов. ну вот конечно у нас не прям провода, а токопроводящие ... (линии вроде, я забыла какое существительное он сказал )

процессор через шину управления говорит какую команду надо выполнить. осталось понять, кому он говорит эту команду. по шине адресса он конкретно передает кому он хочет это сказать (например он адресует это оперативной памяти - дай мне данные, запиши данные). а сами данные идут по шине адреса. 

на самом деле когда говорят про шину данных говорят : процессор на шине данных выставил команду (подал по проводочкам напряжение). еще надо задуматься что 0 это не 0 вольт напржения, потому что тогда не будет ничего работать. 

### архитектура Intel 
схема современного компьютера достаточно сложна.

к процессору подключается не все. часть вещей подключается к chipset - набор чипов. к этому набору подключаются более медленные устройства, так как если они будут подключаться напрямую к процессору, то процессор будет сильно замедляться. 

### system agent
по сути это компьютер внутри процессора. прикол в том что внутри этого систем агента по сути стоит unix, которую написал какой то чел для учебного проекта (не помнью имя было на первой паре)

### схема процессора
сфотографировала

предсказатель переходов - попытка процессора еще не выполнив вычисления понять куда пойдут дальше

процессор выполняет команды последовательно пока не будет команды безусловного перехода. а пот какому адресу мы сейчас обрабатываем команду? у процессора есть специальный регистр - счетчик команд, хотя он и счетчик, на деле там хранится адрес следующей команды. он передает его блоку декодированию, блок инструкций попросил из кэша по адресу данные. кэш говоит извини у меня нет, он пытается обратиться кешу второго уровня, у него тож нет. кеш второго уровня обращается к третьему, у того тоже нет, а потом уже кеш 3 уровня просит у контроллера озу
мы забрали эту инструкцию, идет декодирование инстуркции. возможно для этой инструкции нужны данные, если она принимате аргументы. затем вступают блоки выборки, которые по такому же принципу ищут данные в кеше 1, 2, 3 уровня, озу. 

дальше идет управляющий блок. который понимает что мы выполняем и выполняет это. а куда должен пойти результат? начинается блок сохранения результата. если не было прыжка, то идет переход на следующую инструкцию. это выясняется в момент декодирования команды, так как именно в этот момент выясняется какая команда выполняется и сколько памяти она занимает 

а что прихсодит когда процессор не выполняет команды? иногда конечно он выполняет пустые команды, а так просто не работает (выключается?)

## прерывания

существуют прерывания (когда команда перестает свое выполнение на время)

типы прерываний: 
- внешние - когда например сам пользователь нажимает на клавишу, двигает мышкой - процессору надо обработать это действие мгновенно
- ошибки - в процесс выполнения команды происходит недопустимое действие (например, деление на 0), тогда команда прерывается
- внутренние - благодаря ним работают функции. если goto не сохраняет место вызова и идет просто по адресу, то такое внутреннее прерывание запоминает место, может вызвать функцию извне и продолжить выполнение команды

при прерывании 
- сохраняется контекст
- получается номер прерывания
- находится ISR 
- выполняется ISR

# Лекция 3. 15.09.2026. Понятие процесса. Операции над процессами
Процесс что это? Это последовательное выполнение действий, какие то действия. 
А в чем отличие процесса от алгоритма? Алгоритм это статическое, теоретическое. А процесс это динамическое. 
Например рецепт это алгоритм, а приготовление по рецепту это процесс. 

Процесс сам по себе не существуют, всегда есть ресурсы. Например при готовке нужны ингридиенты, печка и тп и тд

"Программа" - в нашем примере этот термин - это рецепт. При этом не общий алгоритм для всех, а конкретные команды для конкретного исполнителя (конкретный процессор в конкретной архитектуре)

Термин программа не может использоваться для описания происходящего внутри ОС, так как это просто набор байтиков. 

Есть еще один термин, который использовался на заре 60х - "задание". Байка: что нам надо для написания программы? ноут, клавиатура и тп и тп. а как было у программистов 70-80х? программист писал программу на листочке тупа. программист отдает специальному человеку эту программу которую специалист набирает на клавиаутер. устройство создает  по программе написаной дырочки в перфокарте. когда весь текст набран полуается 1 перфокарта - 1 строка кода. после набора программ эта стопка  перфокарт отдается другому специалисту, программист заполняет формуляр (сколько надо памяти, какие нужны ресурсы, какие файлы будут). спеицалист забивает в журнал программиста, говорит ему ляля "я смогу запустить программу завтра в 2 ночи". когда время подошло спецалист эту стопку перфокарт засовывает в компьютер, тот ее загружает в оперативную память. потом оттуда компилятор или тп обрабатывает. запускается программа. вдруг там ошибка например не хватает точки с запятой. формуляр выдает ошибку. бедный программистик бежит с формуляром опять подряд по всем этим чувакам(((
вот этот процесс отдачи стопки перфокарт - "задание" - не модет использоваться для описания происходяшего внутри ос, так как это так же статика

с этого момента мы не будем говорить "запустилась программа", это динамическая весь, мы будем исопльзовать термин процесс. не везде конечно во всех ос называют процессом, но это максимально общий термин (например в линуксе используется термин task)

смотрим вот наш код, не важно на каком языке. то есть у нас в оперативно йпамяти есть набор команд, которые как то туда попали (потом обсудим как именно). в нашей модели договоримся процессор одноядерный. представим что у нас современная ос, нам надо запускать много программ. то есть  вразных кусочках оперативной памяти у нас разные команды. 

процессор выполняет какой то код до тех пор пока его кто то не прервет. а как нам тогда с одной программы переключиться на другую? надо его как то отвлечь, сам он никогда не переключится. допустим в опреативную память мы загрузили 2 команды. а что помимо кода должно быть в оперативной памяти чтобы код запустился

термин "процесс" характеризует совокупность
- набор исполняющихся команд
- ассоциированных с ним ресурсов (данные на обработку)
- текущего момента его выполнения. очень плохая аналогия - мы читаем книжку, зазвонил телефон, что мы делаем? оставляем в книге закладку и по ней возвращаемся обратно. а что у нас текущий момент? сам код. а что пропадает при переключении? значения в регистрах, так что при переключении это значение в регистрах процессор сохраняет в определенном участке оперативной памяти

еще важный момент связанный с исполнением? помните говорили про команды которые есть с процессорами? помним CALL, он сохраняет значения в стэке. 
процесс находтся под управлением ОС

процесс != программа, которая исполняется. кто то может сказать что программа и есть исполняющийся процесс. но есть такие ситуации: 
- для исполнения одной программы может организовываться несколько процессов. типичный пример - веб сервер. запускаетс вроде одна программа, но на деле несколько чтобы обслуживать несколько пользователей
- в рамках одного процесса может исполняться несколько программ. например при использовании библиотек внутри оперативной памяти эти команды библиотек тоже загружаются. это удобно так как если мы в нескольких процессах используем одну и ту же библиотеку, то она загружается один раз в оперативке и потом переиспользуется, то есть своего рода оптимизация
- в рамках процесса может исопльзняться код вне программы (например когда процесс перезаписывает код)

состояния процесса: 
- процесс не исполняется
- исполнение
а какие еще есть состояния? ну вообще по любому если процесс исполняется, то из по любому можно выйти. (например return 0 в C)
на самом деле чтобы процесс завершил свою работу он должен попросить процессор завершения. 

логично что процесс не может завершиться если он не исполняется, так как процесс не исполняемый не может попросить процессор выхода. конечно может вне администратор завешрить процесс, но в линуксе напрмрие некотоыре процессы нельзя завершить, только если перезагрузить ос

еще подумаем какие есть переходы. когда процесс исоплняет по какой то причине он может перестать исполняться, так что у нас есть переход для приостановки. и наборот из не исполненния переход выбран для исполнения. 

ну и еще нужен вход в "процесс не исполняется"

вообще процесс не исполняется можно разделить на 2: 
- готов к исполнению, в любой момент может исполняться и ждет своей очереди
- ождиание (например открытие файла, ожидает сетевого соединения, ожидает ввода из клавиатуры) - например bash poweshell обычно всегда находятся в состоянии ожидания команды от пользователя

когда идет вход в процесс, идет в состояние готовности. когда процессор говорит гого исполняться (там планировщик внутри решает какой код будет исполняться) то там выбран для исполнения, затем исполняется. во время исполнения можно попросить что то от ОС - режим ожидания. еще во время исполнения можно вылететь (прерывание) в готовность если например выделенного времени на исполнения не хватило (типо на одном ядре переключение по 100 мс на разные процессы). для нас то выглядит что это происзодит параллельно но на деле быстро переключается

после ожидания какое то ождиаемое событие произошло, и переход в состояние готовности

переименуем вход в рождение 

получается что процесс это не только код. так как надо хранить состояние, текущий момент исполнения

## операции над процессами
process control block (в нем хранится то что перечислено ниже) и контекст процесса
- состояние процесса
- программный счетчик
- содержимое регистров
- данные для планирования использования процессора и управления памятью
- учетная информация (от имени какого пользователя запустилось, куда имеет право доступа и тп)
- сведения об устройствах ввода-вывода, связаных с процессом. 

## операции над процессами. набор операций
- создание процесс - завершение процесса
- запуск процесса - приостановка процесса
- блокирование процесса - разблокирование процесса (процесс попросил какие то недоступные ресурсы, пока эти ресурсы формируются процесс блокируется)
- изменение приоритета
представим что у нас нет процесса мы его создали, потом есть процесс и мы его завершили. ясно что создание и завершение процесса это разово происходит
так что они называются *одноразовые*, а оставшиеся *многоразовые*

### создание процесса
- порождеине нового PCB с состоянием процесса "ожидание"
- присвоение идентификационного номера (в линукс PID - process ID)
- выделение ресусов: 
    - из ресусорв родителя - от родительского процесса инициирующего данный процесс
    - из ресурсов ос
- занесение в адресное пространтсво код аи установка занчения программного счетчика: 
    - дубликат родителя - копируем от родителя в другую область памяти код
    - из файла - в файле есть другой код и мы его туда загружаем
- окончание заполнения PCB 
- изменеие состояния процесса на "готовность" - это значит процесс отправляется в очередь к планировщику

### завершение процесса
- изменение состояния процесса на "закончил исполнение" - это значит что он сразу выбывает из очереди планировщика
- освобождение ресурсов - закрываются файлы, сетевые соединения, освобождается оперативная память. то есть все из пользовательского контекта
- очистка соответствующих элементов PCB - сам блок не удаляется, только определенные поля, в блоке сохраняется причина завершения. блок не удаляется до тех пор пока родительских процесс не прочитает причину завершения (код завершения), после этого процессор сразу же удалит pcb . пока не прочитано у этого процесса состояние "зомби" - ресурсы не отнимает, но в списках числится

### запуск процесса
- выбор одного из процессов, находящихся в состоянии "готовность". планировщик используя определенные аглоритмы выбирает процесс для выполнения
- изменение состояние выбраноого процесса на "исполнение"
- обеспечение наличия в оперативной памяти информации, необходимой для его выполнения
> файл подкачки - место в диске, куда иногда не исопльзуемые пока данные из оперативки кидаются
- восстановление значений регистров
- передача управления по адресу, на который указывает программный счетчик

### приостановка процесса
- автоматического сохранение программного счетчика и части регистров (работа в Hardware)
- передача управления по специальному адресу (работа Hardware)
- сохраненние динамической части регистрового и системного контекстов в PCB
- обработка прерывания - исполняется код операционной системы который отвечает за эти прерывания (например нажатие клавиши будет обрабатывать драйвер клавиатуры)
- изменеине состояния процесса на "готовность"

### блокиорвание процесса
- сохранение контекста процессор в PCB
- обработка системного вызова
- ...

## разблоикровка
- уточнение того, какое именно событие произошло
- проверка наличия  процесса одижадшего это событиея
- перевод ожидающего процесса в состояние "готовность"
- обрабтка произошедшего события

